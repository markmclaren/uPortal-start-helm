apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uportal-demo-k8s.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "uportal-demo-k8s.name" . }}
    helm.sh/chart: {{ include "uportal-demo-k8s.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "uportal-demo-k8s.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
